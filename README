Run the following line in the terminal to install the Vundle package manager:

    git clone https://github.com/VundleVim/Vundle.vim.git ~/vimfiles/bundle/Vundle.vim

then, while in the lillivimrc directory: 

    cp vimrc ~/_vimrc

next, open Vim and run:
    
    :PluginInstall

maybe run to get rid of old extensions 

    :PluginClean


